#include "holberton.h"

/**
 * _strlen - returns te length of string
 * @s: string length to print
 * Return: length of string
 */

int _strlen(char *s)
{
	int i = 0;

	while (s[i] = '\0')
	{
		i++;
	}
	return (i);
}
